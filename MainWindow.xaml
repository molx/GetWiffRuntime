<Window x:Class="WiffReader.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WIFF Metadata Extractor" Height="560" Width="670" WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        FontFamily="Segoe UI" Background="#F8F9FA"
        Icon="pack://application:,,,/Resources/gemini-svg.ico">

  <Window.Resources>
    <Style TargetType="Button">
      <Setter Property="Background" Value="#0078D4"/>
      <Setter Property="Foreground" Value="White"/>
      <Setter Property="FontSize" Value="14"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
      <Setter Property="Padding" Value="15,5"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border Background="{TemplateBinding Background}" CornerRadius="5" BorderThickness="0">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#106EBE"/>
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="#CCCCCC"/>
                <Setter Property="Foreground" Value="#666666"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style TargetType="TextBox">
      <Setter Property="FontSize" Value="14"/>
      <Setter Property="Padding" Value="8,4"/>
      <Setter Property="BorderBrush" Value="#CED4DA"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="VerticalContentAlignment" Value="Center"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="TextBox">
            <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" CornerRadius="4">
              <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="BorderBrush" Value="#86B7FE"/>
              </Trigger>
              <Trigger Property="IsFocused" Value="True">
                <Setter Property="BorderBrush" Value="#0078D4"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style TargetType="Label">
      <Setter Property="FontSize" Value="14"/>
      <Setter Property="Foreground" Value="#333333"/>
    </Style>

    <Style TargetType="CheckBox">
      <Setter Property="FontSize" Value="14"/>
      <Setter Property="Foreground" Value="#333333"/>
    </Style>
  </Window.Resources>

  <Grid Margin="20">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="Auto"/>
      <ColumnDefinition Width="*"/>
      <ColumnDefinition Width="Auto"/>
    </Grid.ColumnDefinitions>

    <Label Content="Folder Path:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
    <TextBox x:Name="txtPath" Grid.Row="0" Grid.Column="1" Margin="10,0" VerticalAlignment="Center" Height="34"/>
    <Button x:Name="btnBrowse" Content="Browse" Grid.Row="0" Grid.Column="2" Width="95" Height="34" Click="BtnBrowse_Click"/>

    <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,20,0,20">
      <Label Content="Target Year:" VerticalAlignment="Center"/>
      <TextBox x:Name="txtYear" Text="0" Width="70" Margin="10,0" VerticalAlignment="Center" Height="34"/>
      <Label Content="(0 = All Years)" Foreground="#888888" VerticalAlignment="Center"/>
      <CheckBox x:Name="chkMultiThread" Content="SSD Mode (Multi-threading enabled)" IsChecked="False" VerticalAlignment="Center" Margin="40,0,0,0"/>
    </StackPanel>

    <Button x:Name="btnStart" Content="Start Extraction" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" Height="44" Click="BtnStart_Click" Margin="0,0,0,20"/>

    <TextBox x:Name="txtLog" Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="3" IsReadOnly="True" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" Background="Black" Foreground="White" VerticalContentAlignment="Top" FontFamily="Consolas" FontSize="13" Margin="0">
      <TextBox.Template>
        <ControlTemplate TargetType="TextBox">
          <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
            <ScrollViewer x:Name="PART_ContentHost" Margin="0" />
          </Border>
        </ControlTemplate>
      </TextBox.Template>
    </TextBox>
        <ProgressBar x:Name="ExtractionProgressBar" Grid.Row="4" Grid.ColumnSpan="3" Minimum="0" Height="20" Background="#1E1E1E" Foreground="#007ACC" BorderThickness="0" Margin="0,5,0,0" />
    </Grid>
</Window>